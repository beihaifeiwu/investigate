
description = ''
dependencies {
    compile group: 'com.mysema.querydsl', name: 'querydsl-core', version:'3.6.3'
    compile group: 'mysql', name: 'mysql-connector-java', version:'5.1.30'
    compile group: 'org.springframework', name: 'spring-jdbc', version:'4.1.6.RELEASE'
    compile group: 'com.sun.jersey', name: 'jersey-grizzly2', version:'1.19'
    compile(group: 'com.sun.jersey.contribs', name: 'jersey-spring', version:'1.19') {
exclude(module: 'spring-core')
exclude(module: 'spring-web')
exclude(module: 'spring-beans')
exclude(module: 'spring-context')
exclude(module: 'spring-aop')
exclude(module: 'spring-asm')
    }
    compile group: 'com.sun.jersey', name: 'jersey-core', version:'1.19'
    compile group: 'com.sun.jersey', name: 'jersey-server', version:'1.19'
    compile group: 'com.sun.jersey', name: 'jersey-client', version:'1.19'
    compile group: 'com.sun.jersey', name: 'jersey-json', version:'1.19'
    compile group: 'com.owlike', name: 'genson', version:'1.2'
  compile project(':investigate-persistence')
    runtime group: 'org.apache.tomcat', name: 'tomcat-jdbc', version:'8.0.21'
    testCompile group: 'org.springframework', name: 'spring-test', version:'4.1.6.RELEASE'
    testCompile group: 'junit', name: 'junit', version:'4.12'
    compile(group: 'com.mysema.querydsl', name: 'querydsl-sql-codegen', version:'3.6.3') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.springframework', name: 'spring-context', version:'4.1.6.RELEASE') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.springframework', name: 'spring-core', version:'4.1.6.RELEASE') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.springframework', name: 'spring-tx', version:'4.1.6.RELEASE') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
